Defaults !requiretty
<%-
require 'pathname'
plugincmd = Pathname.new(@plugin).absolute? ? @plugin : "#{scope['nagios::client::plugin_dir']}/#{@plugin}"
-%>
<%= scope['nagios::client::nrpe_user'] %> ALL = (<%= @privileged_user %>) NOPASSWD: <%= plugincmd %>
